<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #003458;">
    <div class="container-fluid">
      <a class="navbar-brand" href="/dashboard">SuperBorne</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <% if(user) {%> 
   
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= !borneSelected ? "Borne non sélectionnée": borneSelected.nickname%> 
              
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <% for (let borne of bornes) { %> 
                <% if(borneSelected ) { %> 
                  <% if(borneSelected.id !== borne.id) {  %> 
                    <li><a class="dropdown-item" href="/dashboard/borne/<%= borne.id  %>"><%= borne.nickname %></a></li>
                <% } %> 
                <%  } else { %> 
                  <li><a class="dropdown-item" href="/dashboard/borne/<%= borne.id  %>"><%= borne.nickname %></a></li>

                <% } %> 
              <% } %> 
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="#">Ajouter une borne</a></li>
            </ul>
          </li>
        
        </ul>
      </div>

      <% } %> 
    </div>
  </nav>